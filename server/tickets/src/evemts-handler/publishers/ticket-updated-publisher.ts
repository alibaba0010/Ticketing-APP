import { Publisher, Subjects, TicketUpdatedEvent } from "@alibabatickets/common";

export class TicketUpdatedPublisher extends Publisher<TicketUpdatedEvent> {
  subject: Subjects.TicketUpdated = Subjects.TicketUpdated;
}
